from machine import Pin
import time

# Use GP17 pin on Pi Pico
LED_PIN = 17
led = Pin(LED_PIN, Pin.OUT)

try:
    while True:
        led.value(1)   # LED ON
        time.sleep(1)  # Wait 1 second
        led.value(0)   # LED OFF
        time.sleep(1)  # Wait 1 second
except KeyboardInterrupt:
    print("Program stopped")


OR

from machine import Pin
import time

# Use GPIO 17 (GP17 pin on Pico)
led = Pin(17, Pin.OUT)

while True:
    led.value(1)   # LED ON
    time.sleep(1)  # Wait 1 second
    led.value(0)   # LED OFF
    time.sleep(1)  # Wait 1 second



//https://wokwi.com/projects/439778615279551489
//In simple the project is about - "Turn an LED ON and OFF repeatedly"
//The LED blinks ON/OFF every 1 second continuously.
//When you stop simulation in Wokwi (red square button), the LED stops.
//Since Wokwi doesn’t really use GPIO.cleanup(), the "reset GPIO" part is not applicable.
